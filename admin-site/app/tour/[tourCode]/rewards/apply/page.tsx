import type{Metadata,Viewport}from"next";import RewardApplyClient from"./RewardApplyClient";export const viewport:Viewport={width:"device-width",initialScale:1,viewportFit:"cover",themeColor:"#f7faf7"};export const metadata:Metadata={title:"경품 신청 | 철원 스탬프투어",description:"달성한 경품의 수령 방법과 배송 정보를 입력하세요."};export default async function Page({params}:{params:Promise<{tourCode:string}>}){return <RewardApplyClient tourCode={(await params).tourCode}/>}
