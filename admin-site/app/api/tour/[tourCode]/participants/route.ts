import{registerParticipant}from"@/lib/stamp-tours/service";import{jsonBody,required,routeError}from"@/lib/stamp-tours/http";import{participantCookieName}from"@/lib/stamp-tours/security";
type Body={name:string;phone:string;phoneVerificationId:string;phoneVerificationProof:string;consents:Array<{type:string;policyVersion:string;agreed:boolean}>};
export async function POST(request:Request,{params}:{params:Promise<{tourCode:string}>}){try{const b=await jsonBody<Body>(request);required(b.name,"name");required(b.phone,"phone");required(b.phoneVerificationId,"phoneVerificationId");required(b.phoneVerificationProof,"phoneVerificationProof");const tourCode=(await params).tourCode;const result=await registerParticipant(tourCode,b);const response=Response.json({data:{...result,participantToken:undefined}},{status:201});response.headers.append("set-cookie",`${participantCookieName(tourCode)}=${result.participantToken}; Path=/; Max-Age=2592000; HttpOnly; SameSite=Lax${process.env.NODE_ENV==="production"?"; Secure":""}`);return response;}catch(error){return routeError(error);}}
