# AOS Platform - AGENTS.md

## 1. 프로젝트 개요

AOS Platform은 여행사 업무를 통합 관리하기 위한 Travel ERP Platform이다.

본 시스템은 단순 홈페이지가 아니라 여행상품 등록부터 예약, 고객, 요금, 결제, 정산, 판매점 및 운영관리까지 연결되는 통합 여행 ERP를 목표로 한다.

모든 AI Agent와 개발자는 이 문서를 AOS 프로젝트의 최상위 개발 지침으로 사용한다.

---

## 2. 작업 전 필수 확인

코드를 수정하기 전에 반드시 다음 문서를 확인한다.

1. `AGENTS.md`
2. `docs/PROJECT.md`
3. `docs/rules/UI_RULES.md`
4. `docs/rules/CODING_RULES.md`
5. 작업 대상 기능에 해당하는 `docs/planning/*.md`

작업별 기획문서가 존재하면 해당 문서를 우선 적용한다.

기획문서와 현재 코드가 충돌할 경우 임의로 대규모 수정하지 말고 충돌 내용을 먼저 확인한다.

---

## 3. 핵심 작업 원칙

### 기존 기능 보호

기존에 정상 작동하는 기능을 임의로 삭제하거나 초기화하지 않는다.

요청받지 않은 페이지, 기능, 스타일, 데이터 구조를 임의로 변경하지 않는다.

부분 수정 요청을 전체 페이지 재작성으로 확대하지 않는다.

---

### 공통 구조 보호

다음 공통 영역은 특별한 요청이 없는 한 기존 구조를 유지한다.

- Header
- Sidebar
- Footer
- Navigation
- 공통 CSS
- 공통 JavaScript
- 공통 Modal
- 공통 Form
- 공통 Table

동일한 UI를 페이지마다 새로 만들지 말고 공통 컴포넌트 또는 공통 스타일을 우선 사용한다.

---

## 4. 작업 범위 준수

사용자가 지정한 작업 대상 파일과 기능을 우선 수정한다.

예:

`admin/reservation_calendar.html`

수정 요청이라면 관련 없는 상품관리, 회원관리, 정산관리 등의 파일을 임의로 변경하지 않는다.

관련 파일 변경이 반드시 필요한 경우 변경 이유를 명확하게 판단한 후 최소 범위로 수정한다.

---

## 5. UI/UX 원칙

AOS 관리자 화면은 여행사 실무자가 장시간 사용하는 ERP 시스템이다.

따라서 화려한 디자인보다 다음을 우선한다.

1. 정보 가독성
2. 업무 처리 속도
3. 일관성
4. 실수 방지
5. 검색 편의성
6. 상태 확인 용이성
7. 적절한 정보 밀도

기존 AOS 디자인 시스템을 우선 적용한다.

---

## 6. 반응형 원칙

관리자 시스템은 Desktop 환경을 최우선으로 한다.

기본 기준:

- Desktop 우선
- 1440px 환경 최적화
- 1920px 대응
- 최소 1280px 업무환경 고려
- Tablet은 주요 조회 기능 대응
- Mobile은 필요한 기능에 한해 대응

모바일 대응 때문에 Desktop ERP의 정보 효율성을 떨어뜨리지 않는다.

---

## 7. 데이터 처리 원칙

UI에 표시하는 예약번호, 상품코드, 금액, 고객정보 등의 값은 향후 API 및 DB 연결을 고려한다.

HTML에 업무 데이터를 과도하게 하드코딩하지 않는다.

샘플 데이터가 필요한 경우 실제 데이터와 혼동되지 않도록 명확한 테스트 데이터로 작성한다.

---

## 8. 여행 ERP 핵심 데이터

다음 데이터는 서로 연결될 수 있다는 것을 항상 고려한다.

상품
↓
출발일
↓
요금
↓
좌석/재고
↓
예약
↓
예약자/여행자
↓
결제
↓
취소/환불
↓
판매점
↓
공급사
↓
정산

한 화면 수정으로 다른 업무 데이터의 의미가 깨지지 않도록 한다.

---

## 9. 금액 데이터

금액 관련 값은 의미를 명확하게 구분한다.

예:

- 상품금액
- 옵션금액
- 할인금액
- 총 예약금액
- 총 결제금액
- 미수금
- 환불금액
- 판매수수료
- 공급가
- 정산금액

금액의 의미를 임의로 혼용하지 않는다.

---

## 10. 상태값

상태는 텍스트와 시각적 Badge를 함께 사용할 수 있다.

예:

예약상태
- 예약접수
- 예약확정
- 대기
- 취소요청
- 취소완료

결제상태
- 미결제
- 부분결제
- 결제완료
- 환불진행
- 환불완료

상태를 색상만으로 구분하지 않는다.

---

## 11. 삭제 작업

파일, 기능, CSS, JavaScript 또는 데이터를 삭제하기 전에 사용 여부를 확인한다.

확실하지 않은 코드는 임의로 삭제하지 않는다.

특히 공통 CSS와 공통 JS 삭제에 주의한다.

---

## 12. JavaScript

기존 JavaScript 기능을 유지한다.

이벤트 중복 등록을 피한다.

전역 변수 사용을 최소화한다.

DOM 요소가 존재하는지 확인하고 이벤트를 연결한다.

브라우저 Console Error가 발생하지 않도록 한다.

---

## 13. CSS

가능하면 기존 공통 클래스를 재사용한다.

페이지마다 동일한 스타일을 중복 선언하지 않는다.

`!important` 남용을 금지한다.

인라인 스타일 사용을 최소화한다.

기존 페이지의 레이아웃을 깨뜨릴 가능성이 있는 전역 selector 변경을 피한다.

---

## 14. 작업 완료 전 검증

작업 완료 전 반드시 다음을 확인한다.

- 화면 깨짐 여부
- Header 정상 여부
- Sidebar 정상 여부
- 메뉴 링크
- 버튼 동작
- Modal 열기/닫기
- Form 입력
- 검색 UI
- Table 정렬
- Pagination
- Console Error
- 기존 기능 영향
- 반응형 레이아웃

---

## 15. Git 작업

큰 작업은 가능한 별도 branch에서 진행한다.

예:

`feature/reservation-calendar`

`feature/reservation-detail`

`feature/product-management`

`feature/settlement`

작업과 관계없는 파일을 commit하지 않는다.

Commit message는 변경 목적을 이해할 수 있게 작성한다.

---

## 16. AI Agent 금지사항

AI Agent는 다음 행동을 하지 않는다.

- 요청하지 않은 전체 페이지 재설계
- 프로젝트 초기화
- 기존 코드 대량 삭제
- 정상 기능 임의 제거
- 임의의 프레임워크 변경
- 요청 없는 라이브러리 설치
- 공통 CSS 무분별한 수정
- 기존 메뉴 구조 임의 변경
- 임의 DB 구조 변경
- 테스트 없이 대규모 refactoring

---

## 17. 작업 완료 보고

작업 완료 후 다음 내용을 간단하게 정리한다.

1. 작업한 내용
2. 수정한 파일
3. 새로 추가한 파일
4. 주요 변경사항
5. 테스트 결과
6. 추가 확인이 필요한 사항

---

## 18. 최우선 원칙

AOS에서는

"새롭게 만드는 것"보다
"기존 시스템을 안정적으로 확장하는 것"

을 우선한다.

기존 디자인과 기능을 최대한 유지하면서 필요한 기능만 정확하게 추가하거나 수정한다.