import{applyReward,rewardApplication}from"@/lib/stamp-tours/service";import{jsonBody,ok,routeError}from"@/lib/stamp-tours/http";import{readParticipantToken}from"@/lib/stamp-tours/security";
type Body={rewardId:string;requestKey:string;fulfillmentMethod:"DELIVERY"|"PICKUP"|"MOBILE_COUPON";recipientName:string;phone:string;postalCode?:string;address?:string;addressDetail?:string;deliveryRequest?:string;rewardPrivacyAgreed:boolean;deliveryOutsourcingAgreed:boolean;policyAgreed:boolean};
export async function GET(request:Request,{params}:{params:Promise<{tourCode:string}>}){try{const tourCode=(await params).tourCode;return ok(await rewardApplication(tourCode,readParticipantToken(request,tourCode)));}catch(error){return routeError(error);}}
export async function POST(request:Request,{params}:{params:Promise<{tourCode:string}>}){try{const tourCode=(await params).tourCode;return ok(await applyReward(tourCode,readParticipantToken(request,tourCode),await jsonBody<Body>(request)),201);}catch(error){return routeError(error);}}
